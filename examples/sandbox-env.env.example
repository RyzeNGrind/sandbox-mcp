# Example environment file for sandbox-mcp
# Copy this to sandbox-env.env and customize as needed
# NEVER commit the actual .env file with real secrets!

# Logging configuration
SANDBOX_LOG_LEVEL=info
SANDBOX_LOG_FORMAT=json

# Execution limits  
SANDBOX_DEFAULT_TIMEOUT=60
SANDBOX_MAX_MEMORY=512M
SANDBOX_MAX_CPU_CORES=2

# Nix configuration
NIX_PATH=nixpkgs=<nixpkgs>
SANDBOX_NIX_STORE_PATH=/nix/store

# Optional: API keys or tokens for enhanced functionality
# Replace with actual values or use passwordCommand instead
# API_KEY=your_api_key_here
# AUTH_TOKEN=your_auth_token_here

# Optional: Custom sandbox directories
# SANDBOX_WORK_DIR=/tmp/sandbox-workdir
# SANDBOX_CONFIG_DIR=/etc/sandbox-mcp